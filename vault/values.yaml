vault:
  # Directory data volume is mounted in pod
  dataMount: /mnt/data

  # Address to listen on
  address: 0.0.0.0:8200

  # Host
  host: vault.kscout.io

# Vault API controller configuration
vac:
  imageVersion: v0.1.2

  # Vault API token, if empty VAC will run in bootstrap mode (just sit there)
  #vaultServerToken: ""
